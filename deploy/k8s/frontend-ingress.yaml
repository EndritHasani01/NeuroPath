apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adaptive-learning-frontend
  namespace: neuropath
spec:
  ingressClassName: traefik
  rules:
    - host: neuropath.localtest.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adaptive-learning-frontend
                port:
                  number: 80
