apiVersion: v1
kind: Service
metadata:
  name: adaptive-learning-frontend
  namespace: neuropath
spec:
  type: ClusterIP
  selector:
    app: adaptive-learning-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
