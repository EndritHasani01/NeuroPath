apiVersion: v1
kind: Service
metadata:
  name: adaptive-learning-backend
  namespace: neuropath
spec:
  type: ClusterIP
  selector:
    app: adaptive-learning-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
