apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adaptive-learning-llm
  namespace: neuropath
spec:
  ingressClassName: traefik
  rules:
    - host: llm.neuropath.localtest.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adaptive-learning-llm
                port:
                  number: 8000
